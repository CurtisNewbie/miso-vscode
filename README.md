# miso-vscode

```sh
npm install -g @vscode/vsce
vsce package
```

```json
"editor.tokenColorCustomizations": {
    "textMateRules": [
        {
            "scope": "keywords.misoapi",
            "settings": {
                "fontStyle": "italic bold"
            }
        }
    ]
}
```
